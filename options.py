import json
from pprint import pprint


short_options = {
  # 'v': 'verbose',
  # 'q': 'quiet',
  # 'c': 'checksum',
  # 'a': 'archive',
  # 'r': 'recursive',
  # 'R': 'relative',
  # 'b': 'backup',
  # 'u': 'update',
  # 'd': 'dirs',
  # 'l': 'links',
  # 'L': 'copy-links',
  # 'H': 'hard-links',
  # 'K': 'keep-dirlinks',
  # 'p': 'perms',
  # 'o': 'owner',
  # 'g': 'group',
  # 'D': 'devices',
  # 't': 'times',
  # 'O': 'omit-dir-times',
  # 'S': 'sparse',
  # 'n': 'dry-run',
  # 'W': 'whole-file',
  # 'x': 'one-file-system',
  # 'B': 'block-size',
  # 'e': 'rsh',
  # 'I': 'ignore-times',
  # 'T': 'temp-dir',
  # 'y': 'fuzzy',
  # 'z': 'compress',
  # 'C': 'cvs-exclude',
  # 'f': 'filter',
  # 'F': None,
  # '0': 'from0',
  # 'P': ('partial', 'progress'),
  # 'i': 'itemize-changes',
  # '4': 'ipv4',
  # '6': 'ipv6',
  # 'h': 'help',
  # 'v': 'verbose',
  # '4': 'ipv4',
  # '6': 'ipv6',
  # 'h': 'help',
}

long_options = {
  # 'verbose': 'v',
  # 'quiet': 'q',
  # 'checksum': 'c',
  # 'archive': 'a',
  # 'recursive': 'r',
  # 'relative': 'R',
  # 'no-relative': None,
  # 'no-implied-dirs': None,
  # 'backup': 'b',
  # 'backup-dir': None,
  # 'suffix': None,
  # 'update': 'u',
  # 'inplace': None,
  # 'dirs': 'd',
  # 'links': 'l',
  # 'copy-links': 'L',
  # 'copy-unsafe-links': None,
  # 'safe-links': None,
  # 'hard-links': 'H',
  # 'keep-dirlinks': 'K',
  # 'perms': 'p',
  # 'owner': 'o',
  # 'group': 'g',
  # 'devices': 'D',
  # 'times': 't',
  # 'omit-dir-times': 'O',
  # 'sparse': 'S',
  # 'dry-run': 'n',
  # 'whole-file': 'W',
  # 'no-whole-file': None,
  # 'one-file-system': 'x',
  # 'block-size': 'B',
  # 'rsh': 'e',
  # 'rsync-path': None,
  # 'existing': None,
  # 'ignore-existing': None,
  # 'remove-sent-files': None,
  # 'del': None,
  # 'delete': None,
  # 'delete-before': None,
  # 'delete-during': None,
  # 'delete-after': None,
  # 'delete-excluded': None,
  # 'ignore-errors': None,
  # 'force': None,
  # 'max-delete': None,
  # 'max-size': None,
  # 'partial': None,
  # 'partial-dir': None,
  # 'delay-updates': None,
  # 'numeric-ids': None,
  # 'timeout': None,
  # 'ignore-times': 'I',
  # 'size-only': None,
  # 'modify-window': None,
  # 'temp-dir': 'T',
  # 'fuzzy': 'y',
  # 'compare-dest': None,
  # 'copy-dest': None,
  # 'link-dest': None,
  # 'compress': 'z',
  # 'cvs-exclude': 'C',
  # 'filter': 'f',
  # 'exclude': None,
  # 'exclude-from': None,
  # 'include': None,
  # 'include-from': None,
  # 'files-from': None,
  # 'from0': '0',
  # 'version': None,
  # 'address': None,
  # 'port': None,
  # 'blocking-io': None,
  # 'no-blocking-io': None,
  # 'stats': None,
  # 'progress': None,
  # 'itemize-changes': 'i',
  # 'log-format': None,
  # 'password-file': None,
  # 'list-only': None,
  # 'bwlimit': None,
  # 'write-batch': None,
  # 'read-batch': None,
  # 'protocol': None,
  # 'checksum-seed': None,
  # 'ipv4': '4',
  # 'ipv6': '6',
  # 'help': 'h',
  # 'daemon': None,
  # 'address': None,
  # 'bwlimit': None,
  # 'config': None,
  # 'no-detach': None,
  # 'port': None,
  # 'verbose': 'v',
  # 'ipv4': '4',
  # 'ipv6': '6',
  # 'help': 'h',
}

with open('options.json') as f:
  options = json.load(f)
  short_options = options[u'short']
  long_options = options[u'long']
  
  if __name__ == '__main__':
    pprint(short_args)
    pprint(u'--------')
    pprint(long_args)